type Query {
  meals: [Meal!]!
  categories: [Category!]!
}

type Meal {
  id: ID!
  name: String
  price: Float
  quantity: Int
  category: Category
}

type Category {
  id: ID!
  name: String
  meals: [Meal!]
}
